// Small standalone component (unused by the single-file app but provided for expandability)
export default function TranscriptForm(props) {
  // This file is intentionally minimal — the main app is in src/app.js.
  return null;
}
